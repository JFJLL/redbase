# 09-01 Plan: Move Admin Overview To SQL Repositories

**Status:** Passed
**Created:** 2026-05-01

## Scope

- Remove `/api/admin/overview` and admin mutation response refreshes from full snapshot reads.
- Keep response compatibility with the existing admin frontend.
- Keep verification-code behavior unchanged.

## Implementation Checklist

- [x] Add repository helpers for SQL-backed overview data.
- [x] Replace admin route `readStore()` overview calls.
- [x] Run `npm run check`.
- [x] Run `npm test`.
- [x] Run API smoke.
- [x] Run real browser login verification.

## Verification

```powershell
npm run check
npm test
$env:SMOKE_BASE_URL='http://127.0.0.1:3024'; npm run smoke:api
```
